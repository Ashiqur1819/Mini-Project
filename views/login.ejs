<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-800 p-10 text-white mx-auto">
      <h2 class="text-3xl mb-6 text-center">Login Your Account</h2>
      <form
        action="/login"
        method="post"
        class="w-1/4 mx-auto flex flex-col gap-6 flex-wrap"
      >
        <input
          class="px-5 py-2 border-1 border-zinc-700 rounded-md outline-none"
          type="email"
          placeholder="Email"
          name="email"
        />
        <input
          class="px-5 py-2 border-1 border-zinc-700 rounded-md outline-none"
          type="password"
          placeholder="Password"
          name="password"
        />
        <input
          class="px-5 py-2 rounded-md bg-orange-500 cursor-pointer"
          type="submit"
          value="Login Account"
        />
      </form>
    </div>
  </body>
</html>
