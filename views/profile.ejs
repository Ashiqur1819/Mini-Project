<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-800 p-10 text-white">
      <div>
        <div class="flex justify-end">
          <a
            href="/logout"
            class="px-5 py-2 rounded-md bg-red-500 cursor-pointer"
            >Logout</a
          >
        </div>
        <div class="mb-6">
          <h2 class="text-3xl mb-1">
            Welcome <span class="text-orange-500"><%= user.username %></span> 👋
          </h2>
          <p class="text-zinc-200">Now, You can create a post.</p>
        </div>
        <form
          action="/post"
          method="post"
          class="w-1/3 flex flex-col gap-3 flex-wrap"
        >
          <textarea
            name="content"
            id=""
            class="px-5 py-2 border-1 border-zinc-700 rounded-md outline-none resize-none w-full"
            placeholder="What's on your mind?"
          ></textarea>
          <input
            class="px-5 py-2 w-52 rounded-md bg-orange-500 cursor-pointer"
            type="submit"
            value="Create Post"
          />
        </form>
      </div>

      <div class="mt-20">
        <h3 class="text-2xl">Your All Posts</h3>
        <div class="mt-6 flex gap-6">
          
           <% user.posts.reverse().forEach((post) => { %>
  <div class="bg-zinc-700 w-1/4 p-4 rounded-md">
    <h3 class="text-xl text-sky-400 mb-1"><%= user.username %></h3>
    <p class="text-zinc-300">
      <%= post.content %>
    </p>
    <p class="mt-2"><%= post.likes.length %> likes</p>
    <div class="mt-6 flex gap-6">
      <a href="/like/<%= post._id %>" class="text-orange-500"><%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %></a>
      <a href="" class="text-sky-500">Edit</a>
    </div>
  </div>
<% }) %>


        </div>
      </div>
    </div>
  </body>
</html>
